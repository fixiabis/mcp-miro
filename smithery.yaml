startCommand:
  type: docker
  configSchema:
    type: object
    properties:
      miroToken:
        type: string
        description: "Miro OAuth token for accessing Miro API"
        secret: true
    required: ["miroToken"]
  env:
    MIRO_OAUTH_TOKEN: ${config.miroToken}
  args: [] 